/*
import express from 'express';
import getManyUsers from '../controllers/product/getManyUsers.js';
import createUser from '../controllers/product/createUser.js';
import getUser from '../controllers/product/getUser.js';
import deleteUser from '../controllers/product/deleteUser.js';
import updateUser from '../controllers/product/updateUser.js';

const router = express.Router();

router.get('/product', getManyProduct); // *** get all
router.get('/product/:id', getProduct); // *** get one by id
router.post('/product', createProduct); // *** create a new one
router.delete('/product/:id', deleteProduct); // *** delete one
router.patch('/product/:id', updateProduct); // *** modify one

export default router;
*/
